name: Test a simple push action
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: "curl -i -X POST  -d '${{ github.event_name }} on ${{ github.repository }}. ${{ github.sha }}' https://galenus-verbatim.huma-num.fr/test/github.php"
